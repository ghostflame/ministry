Stats generation
	Allow for usec timestamps - multiple buffers

Monitoring
	carbon-copy self-metrics

Operational Readiness

	Pen Testing
    review:  https://cwe.mitre.org/data/definitions/658.html
		done:	https://cwe.mitre.org/data/definitions/14.html

Carbon-Copy
    Crashing with multiple sources (from ministry test)

Clustering
	Make sure existing connections are not closed until shutdown
	This involves separting shutting down from RUNNING

Filtering app?
	Respond to a drop-in dir of configs
	Json objects:
	{
		enabled: <bool>,
		ips: [
			<ip> | <range>,
			...
		],
		type: <all|allow|drop>, ( allow all, allow matching, drop matching )
		matches: [
			/regex,
			...
		]
	}
	anything not matching is dropped
	forward to a single target
	ongoing connection per thread, write at once

Consul registration?
	https://www.consul.io/api/agent/service.html#register-service

Slack integration?
	Send warns/errors to slack?
	Or specific slack messages
	Slack thread, with messaging memhl
	How to avoid fail-loop?
		generate err
		fail to send
			generates err
				fails to send...

Prometheus metrics

